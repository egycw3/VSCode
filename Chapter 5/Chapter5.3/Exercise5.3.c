#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    // Declare some variables
    int a = 1;
    char letterb = '2';
    float f = 1.23f;

    // Use printf display text on the screen
    printf ("The variables are\na = %d\nb = %c\nf = %f", a, letterb, f);

    // Exit from main
    return 0;
}